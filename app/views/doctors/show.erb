<h1><%= @doctor.name %></h1>

<ul>
    <% @doctor.appointments.each do |app|%>
        <li><%= app.appointment_datetime.strftime("%B %d, %Y at %k:%M") %></li>
        <li><%= link_to app.patient.name, patient_path(app.patient.id)%></li><br>
    <%end%>

</ul>